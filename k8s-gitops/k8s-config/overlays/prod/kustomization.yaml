apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: prod

resources:
  - namespace.yaml
  - secrets.yaml
  - ../../base/database
  - ../../base/messaging
  - ../../base/app

patches:
  - path: service-patch.yaml
    target:
      kind: Service
      name: local-dev-app
