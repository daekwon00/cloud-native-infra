apiVersion: v1
kind: Service
metadata:
  name: local-dev-app
  labels:
    app: local-dev-app
spec:
  type: NodePort
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
  selector:
    app: local-dev-app
